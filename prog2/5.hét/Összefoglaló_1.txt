#include "boost/filesystem/operations.hpp" // includes boost/filesystem/path.hpp
#include "boost/filesystem/fstream.hpp"    
#include "boost/filesystem.hpp"
#include <iostream>                        

Programkódunk ezen részében hozzácsatoljuk a szükséges header fileokat.

if (exists(p)) {
      
      if (is_regular_file(p)){
        cout << p << " size is " << file_size(p) << '\n';
       
       if (getExt(p) == ".java"){
       		counter++;
       		
       }
    }

A következõ részben pedig elkezdjük a sima fájlok és a .java kiterjesztésû fájlok számlálását.
Ha ezek az ifek nem teljesülnek azaz nincsenek további fájlok ,akkor megvizsgáljuk a mappák almappáit.
Így az almappákban a folyamat újraindul.

else if (is_directory(p)) {
        cout << p << " is a directory containing:\n";

        for (directory_entry& x : directory_iterator(p)){
        
          cout << "    " << x.path() << '\n'; 
          dirent(x.path());
      		
        }
      }

A programcódunk fõ részében pedig megvizsgáljuk, hogy helyesen van e megadva a bemenet és ha igen akkor kiírjuk a jelenlegi 
elérési útvonalat. Ezután pedig kiírjuk a megszámolt .java kiterjesztésû fájlok számát.